import { PipePropioPipe } from './pipe-propio.pipe';

describe('PipePropioPipe', () => {
  it('create an instance', () => {
    const pipe = new PipePropioPipe();
    expect(pipe).toBeTruthy();
  });
});
